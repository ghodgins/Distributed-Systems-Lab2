Distributed-Systems-Lab2
========================

Skeleton Server written in Haskell

To install the Haskell Platform:
  - Ubuntu -> sudo apt-get install haskell-platform (should install all packages needed and the ghc compiler)

Run "./compile.sh" and "start.sh [port]" to compile and start the server.